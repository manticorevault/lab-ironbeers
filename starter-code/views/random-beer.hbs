<div>
    {{#each randomBeer}} {{!--  --}}
        <div>
            <img src="{{this.image_url}}" alt="a picture of a beer">
        </div>

        <div>
            <h1>{{this.name}}</h1>
            <h2>{{this.tagline}}</h2>
            <p>{{this.description}}</p>
        </div>
    {{/each}}
</div>